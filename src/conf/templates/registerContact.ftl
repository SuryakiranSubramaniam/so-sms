{ "customerId": "${body.customerId}", "journeyId": "${body.journeyId}", "activityId": "${body.activityId}", "notificationChannel": "<#if (body.channel?has_content) > ${body.channel}<#else>SMS</#if>", "eventTimestamp": <#if (body.triggerTimestamp?has_content)> ${body.triggerTimestamp?c} <#else> null </#if>, "offerType": <#if (body.offerType?has_content)>"${body.offerType}" <#else> null </#if>, "category": "<#if (body.category?has_content) >${body.category}<#else>null</#if>", "triggerSource": "<#if (body.triggerSource?has_content) >${body.triggerSource}<#else>null</#if>", "contactId": <#if (body.contactId?has_content)>"${body.contactId}"<#else>null</#if>, "subscriberId": "${body.subscriberId}", "programId": "${body.programId}", "offerId": "${body.offerId}", "serviceNumber": <#if (body.serviceNumber?has_content)>"${body.serviceNumber}"<#else>null</#if>, "eventId": <#if body.triggerSource=="MC">"${body.deData.EVENT_ID}"<#else>"<#if (body.eventId?has_content) >${body.eventId}<#else>null</#if>"</#if>, "selectedOfferCategory": "<#if (body.category?has_content) >${body.category}<#else>null</#if>", "flowId": <#if body.triggerSource=="MC">"${body.deData.FLOW_ID}"<#else>"<#if (body.flowId?has_content) >${body.flowId}<#else>null</#if>"</#if>, "countAsContact": <#if body.triggerSource=="MC">true<#else>false</#if>, "requestId": "${body.requestId}", "optInShortCode": <#if body.optInShortCode?has_content>"${body.optInShortCode}"<#else>null</#if>, "eventDefinitionKey": "<#if (body.eventDefinitionKey?has_content) >${body.eventDefinitionKey}<#else>null</#if>", "status": "${body.status?upper_case}", "orgId": <#if (body.orgId?has_content)>"${body.orgId}"<#else>null</#if>, "buId": <#if (body.buId?has_content)>"${body.buId}"<#else>null</#if>, "applyContactPolicy": true, "activityType": <#if (body.activityType?has_content)>"${body.activityType}"<#else>null</#if>, "activityInstanceId": <#if (body.activityType?has_content)>"${body.activityType}"<#else>null</#if>, "isControl": <#if (body.isControl?has_content)>${body.isControl?c}<#else>false</#if>, "isSimulated": <#if (body.isSimulated?has_content)>${body.isSimulated?c}<#else>false</#if>, "mode": "<#if body.status?upper_case=="SUCCESS">incr<#else>decr</#if>" }