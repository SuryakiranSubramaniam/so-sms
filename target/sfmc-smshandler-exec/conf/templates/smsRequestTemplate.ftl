{"definitionKey":"${body[0].messageDefinitionKey}","recipients":[<#list body as map>{"to":"${map.msisdn}","contactKey":"${map.contactId}","messageKey": "${map.flowId}",<#if map.notificationMsg?has_content>"content":{"message":"${map.notificationMsg}"}<#else>"attributes":{<#list map.deData as deDataKey,deDataValue>"${deDataKey}":"${deDataValue}"<#sep>,</#sep></#list>}</#if>}<#sep>,<#sep></#list>]}${request.setHeader('CamelHttpMethod', 'POST')}${request.setHeader('Content-Type', 'application/json')}